server:
  port: 8003

spring:
  application:
    name: ribbon-consumer

spring:
  cloud:
    config:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/

management:
  endpoints:
    web:
      exposure:
        include:
          - hystrix.stream

